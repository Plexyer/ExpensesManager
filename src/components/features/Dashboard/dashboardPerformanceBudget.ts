export const DASHBOARD_PERFORMANCE_BUDGET = {
  criticalWidgetsReadyMs: 400,
  deferredWidgetsReadyMs: 1200,
  periodAndGridBootstrapRequests: 2,
} as const;

export const DASHBOARD_DEFERRED_WIDGET_DELAY_MS = 120;
